#include<iostream>
#include<string>
using namespace std;
int main()
{
  int l,f=1;
  string s,s1;
  getline(cin,s);
  while(s.at(0) != '#' && f)
  {
    cout<<s<<"\n";
    s1 = s;
    l = s1.size();
  getline(cin,s);
    if(s.at(0) != s1.at(l-1))
      f=0;
  }
  return 0;
}
