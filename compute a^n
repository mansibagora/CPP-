#include<iostream>
using namespace std;
int pow(int a,int n)
{
  int p=1;
for(int i=1;i<=n;i++)
  p=p*a;
  return p;
}
int main()
{
  int a,n;
  cin>>a>>n;
  cout<<"Enter the value of a\nEnter the value of n\n";
  cout<<"The value of "<<a<<" power "<<n<<" is "<<pow(a,n);
  return 0;
}
