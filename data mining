#include<iostream>
using namespace std;
int main()
{
  int n,r,e=0,o=0;
  std::cin>>n;
  for(int i=n;i>0;i=i/10)
  {
  r = i%10;
    if(r%2 == 0)
      e = e + r;
    else
      o = o+r;
  }
  if(e==o)
    std::cout<<"Yes";
  else
    std::cout<<"No";
}
